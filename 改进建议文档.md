# 解方程练习生成器 — 改进建议文档

> **使用说明**：本文档可直接提供给 AI 编程助手（Claude Code / Cursor / Copilot 等），逐任务进行改进。每个任务都包含明确的"问题—目标—文件—具体做法"，AI 可以按照本指南独立完成修改。

---

## 目录

- [P0-1: 重构巨型 switch-case 为策略模式](#p0-1-重构巨型-switch-case-为策略模式)
- [P0-2: 消除前端/后端搜索逻辑重复](#p0-2-消除前端后端搜索逻辑重复)
- [P1-1: 题型数据外置为 JSON 配置文件](#p1-1-题型数据外置为-json-配置文件)
- [P1-2: 路径遍历防护加固](#p1-2-路径遍历防护加固)
- [P1-3: 提取魔法数字为命名常量](#p1-3-提取魔法数字为命名常量)
- [P1-4: 补充边界条件测试](#p1-4-补充边界条件测试)
- [P2-1: 添加 JSDoc 类型注释](#p2-1-添加-jsdoc-类型注释)
- [P2-2: 修复 innerHTML 安全隐患](#p2-2-修复-innerhtml-安全隐患)
- [P2-3: 添加安全 HTTP 头和速率限制](#p2-3-添加安全-http-头和速率限制)
- [P2-4: 添加请求速率限制](#p2-4-添加请求速率限制)
- [P3-1: 改进无障碍性](#p3-1-改进无障碍性)
- [P3-2: 改进加载状态和错误提示](#p3-2-改进加载状态和错误提示)

---

## P0-1: 重构巨型 switch-case 为策略模式

### 问题

`src/generator.js` 第 704-1287 行的 `createEquationByType` 函数包含一个 **~580 行的 switch 语句**，处理全部 56 种题型。这是项目最核心的技术债务，导致：

- 修改任一题型需要触碰巨型函数，风险高
- 无法对单个题型做独立的单元测试
- `default` 分支静默回退到 `multiply_add_two_step`，掩盖了配置错误
- 代码可读性差，新贡献者难以入手

### 目标

将每个题型的生成逻辑拆分为独立函数，通过 `Map<typeId, generator>` 查表调用。

### 涉及文件

- **新建** `src/type-generators/` 目录
- **修改** `src/generator.js`
- **修改** `tests/generator.test.js`（确保现有测试仍然通过）

### 具体做法

#### 步骤 1：创建题型生成器注册机制

在 `src/generator.js` 顶部，将 switch 语句替换为一个注册表 `Map`：

```javascript
// 在 generator.js 中新增
const typeGenerators = new Map();

function registerGenerator(typeId, generatorFn) {
  typeGenerators.set(typeId, generatorFn);
}
```

#### 步骤 2：为每个题型创建独立生成器文件

在 `src/type-generators/` 目录下按题型分类创建文件，例如：

```
src/type-generators/
├── index.js              # 导出所有生成器并注册
├── foundation.js         # 方程概念与等式性质（4 个题型）
├── one-step.js           # 一步方程（4 个题型）
├── special-position.js   # 未知数特殊位置（3 个题型）
├── two-step.js           # 两步方程（4 个题型）
├── brackets.js           # 括号整体（4 个题型）
├── combine-terms.js      # 合并同类项（3 个题型）
├── x-both-sides.js       # 两边都有 x（3 个题型）
├── decimal-fraction.js   # 小数/分数（3 个题型）
├── percent-ratio.js      # 百分数/比例（3 个题型）
├── clear-denominator.js  # 含分母/去分母（3 个题型）
├── comprehensive.js      # 一元一次方程综合（3 个题型）
├── check-diagnose.js     # 检验与易错诊断（8 个题型）
└── word-problems.js      # 列方程应用题（10 个题型）
```

每个生成器函数的签名统一为：

```javascript
// 以 one-step.js 中的加法一步方程为例
function generateAddOneStep(type, answer, includeSolution) {
  const a = randomInt(3, 20);
  const equation = makeEquation(1, a, 0, answer + a);
  const solution = `两边同时减去 ${a}，得到 x = ${answer}。`;
  return buildProblem(type, equation, answer, solution, includeSolution);
}
```

然后在 `index.js` 中统一注册：

```javascript
const { registerGenerator } = require("../generator-core"); // 或保留在 generator.js

registerGenerator("add_one_step", generateAddOneStep);
registerGenerator("subtract_one_step", generateSubtractOneStep);
// ... 56 个注册
```

#### 步骤 3：简化 createEquationByType

重构后，原来的 `createEquationByType` 变为一行：

```javascript
function createEquationByType(type, includeSolution) {
  const answer = randomInt(2, 18);
  const generator = typeGenerators.get(type.id);
  if (!generator) {
    // 回退到最安全的默认题型
    return typeGenerators.get("multiply_add_two_step")(type, answer, includeSolution);
  }
  return generator(type, answer, includeSolution);
}
```

#### 步骤 4：确保设计模式与原有逻辑完全一致

重构时必须确保以下通用函数被所有生成器共享（可提取到 `src/generator-utils.js`）：

- `buildProblem(type, equation, answer, solution, includeSolution, question, extra)`
- `makeEquation(leftX, leftConstant, rightX, rightConstant)`
- `renderEquation(equation)`
- `solveLinear(equation)`
- `formatNumber(value)`
- `formatSignedConstant(value)`
- `formatLinearExpression(coefficient, constant)`
- `randomInt(min, max)`
- `pick(items)`
- `toPositiveInt(value, fallback, min, max)`

#### 步骤 5：关键注意点

- `case "multiply_add_two_step":` 和 `case "operation_consistency":` 在原始代码中**共享同一个 case body**。重构时需要明确这是有意设计（两个题型确实生成同类型题目），让它们各自注册但指向同一个生成器函数。
- `divide_one_step`、`divide_then_multiply`、`fraction_coefficient`、`fraction_term_equation` 这几个题型在 switch 中使用了 `return buildProblem(...)` 提前返回（未走到末尾的 `solveLinear` 调用），重构时保持这个模式。
- 原始 `default` 分支回退到 `multiply_add_two_step`——重构后保留这个行为。

#### 验证方式

```bash
npm test
```

所有现有测试必须 100% 通过。特别注意 `generates problems for each specific vertical type` 这个测试会遍历全部 56 种题型。

---

## P0-2: 消除前端/后端搜索逻辑重复

### 问题

`inferTypeIdsFromEquation` 函数在 `src/generator.js:649-670` 和 `public/app.js:117-138` 中**完全相同**。任何搜索策略修改需同步两处，容易遗漏。

### 目标

搜索逻辑统一到服务端，前端只做展示。

### 涉及文件

- **修改** `src/generator.js`
- **修改** `server.js`
- **修改** `public/app.js`

### 具体做法

#### 方案 A（推荐）：搜索完全服务端化

1. 在 `server.js` 中新增 GET 端点 `/api/search?q=关键词`：

```javascript
if (request.method === "GET" && requestUrl.pathname === "/api/search") {
  const query = requestUrl.searchParams.get("q") || "";
  const results = searchTypes(query);  // 复用 generator.js 中已有的 searchTypes
  sendJson(response, 200, { results });
  return;
}
```

2. 修改 `public/app.js` 中的 `renderSearchResults`：

```javascript
async function renderSearchResults() {
  const query = searchInput.value.trim();
  if (!query) { /* 显示默认提示 */ return; }

  try {
    const response = await fetch(`/api/search?q=${encodeURIComponent(query)}`);
    const data = await response.json();
    // 用 data.results 渲染搜索结果
  } catch (error) {
    searchResults.textContent = "搜索失败，请重试。";
  }
}
```

3. 从 `public/app.js` 中**删除** `inferTypeIdsFromEquation`、`searchCatalog`、`normalizeSearchText`（如果只在搜索中用到）函数。

#### 方案 B（轻量方案）：搜索逻辑仍在前端，但通过 API 获取服务端计算的搜索结果

如果不想增加新的 API 端点，可以将 `inferTypeIdsFromEquation` 的结果作为 `/api/generate` 或 `/api/catalog` 响应的一部分返回。但这样耦合度较高，不如方案 A 干净。

#### 关键注意

- 服务端 `searchTypes` 函数返回的结果包含 `keywords` 数组，这可能使响应体积较大。可考虑在前端搜索结果中去掉 `keywords` 字段。
- 确保 URL 查询参数正确编解码中文。

---

## P1-1: 题型数据外置为 JSON 配置文件

### 问题

`TYPE_CATALOG`（56 个题型定义）和 `TYPE_KEYWORD_ALIASES`（~135 个关键词映射）全部硬编码在 `src/generator.js` 中。非开发人员（如教研老师）无法审核或调整题型分类。

### 目标

将题型定义和关键词别名提取为独立 JSON 文件，`generator.js` 在模块加载时读取。

### 涉及文件

- **新建** `src/data/type-catalog.json`
- **新建** `src/data/keyword-aliases.json`
- **修改** `src/generator.js`

### 具体做法

#### 步骤 1：创建 `src/data/type-catalog.json`

将 `TYPE_CATALOG` 数组完整复制到此文件。注意 JSON 不支持函数或模板字面量，但 `TYPE_CATALOG` 目前都是静态字符串数据，完全兼容。

```json
{
  "categories": [
    {
      "id": "foundation",
      "name": "方程概念与等式性质",
      "description": "先练方程的意义、方程的解、等式性质和数量关系列式。",
      "types": [
        {
          "id": "identify_equation",
          "name": "判断是不是方程",
          "pattern": "expression with unknown",
          "focus": "理解含有未知数的等式才是方程。",
          "examples": ["判断 3x + 5 = 20 是不是方程"]
        }
        // ... 其余题型
      ]
    }
    // ... 其余分类
  ]
}
```

#### 步骤 2：创建 `src/data/keyword-aliases.json`

将 `TYPE_KEYWORD_ALIASES` 对象完整复制到此文件：

```json
{
  "identify_equation": ["方程概念", "是不是方程", "判断方程", "含未知数", "等式"],
  "check_equation_solution": ["方程的解", "判断解", "代入", "是不是解", "检验"]
  // ... 其余别名
}
```

#### 步骤 3：修改 `src/generator.js`

```javascript
const fs = require("node:fs");
const path = require("node:path");

const TYPE_CATALOG = JSON.parse(
  fs.readFileSync(path.join(__dirname, "data", "type-catalog.json"), "utf-8")
).categories;

const TYPE_KEYWORD_ALIASES = JSON.parse(
  fs.readFileSync(path.join(__dirname, "data", "keyword-aliases.json"), "utf-8")
);
```

#### 关键注意

- `readFileSync` 在模块顶层执行，只在首次 `require` 时运行一次，不影响运行时性能。
- JSON 文件格式必须严格——不能有尾随逗号、不能有注释（或使用 JSON5 库）。
- 如果考虑热更新需求，可改用 `fs.readFile` 异步读取 + 缓存机制，但当前 MVP 不需要。

---

## P1-2: 路径遍历防护加固

### 问题

`server.js:50-56` 的路径遍历防护在 Windows 上存在潜在绕过风险：
- URL 编码的 `..%5c..%5c` 可能绕过正则 `replace`
- Windows 路径规范化在不同版本有差异

### 目标

加固 `serveStatic` 函数的路径安全。

### 涉及文件

- **修改** `server.js`

### 具体做法

修改 `server.js` 的 `serveStatic` 函数：

```javascript
function serveStatic(request, response) {
  const requestUrl = new URL(request.url, "http://localhost");
  const pathname = requestUrl.pathname === "/" ? "/index.html" : requestUrl.pathname;

  // 步骤 1：URL 解码（防止编码绕过）
  const decoded = decodeURIComponent(pathname);

  // 步骤 2：规范化路径
  const normalized = path.normalize(decoded);

  // 步骤 3：禁止路径遍历字符（防御深度）
  if (normalized.includes("..")) {
    response.writeHead(403);
    response.end("Forbidden");
    return;
  }

  // 步骤 4：只允许安全字符（白名单）
  if (!/^[/\\][a-zA-Z0-9_\-./\\]*$/.test(normalized)) {
    response.writeHead(403);
    response.end("Forbidden");
    return;
  }

  const filePath = path.join(PUBLIC_DIR, normalized);

  // 步骤 5：最终检查必须在 PUBLIC_DIR 内
  if (!filePath.startsWith(PUBLIC_DIR + path.sep)) {
    response.writeHead(403);
    response.end("Forbidden");
    return;
  }

  // ... 后续 fs.readFile 不变
}
```

#### 关键注意

- `path.sep` 在 Windows 上是 `\`，Linux 上是 `/`。`startsWith(PUBLIC_DIR + path.sep)` 比 `startsWith(PUBLIC_DIR)` 更严格（防止 `PUBLIC_DIR-other` 这类路径）。
- 如果项目需要支持中文文件名（如 `题型.html`），需要调整正则白名单以包含 Unicode 字符范围。

---

## P1-3: 提取魔法数字为命名常量

### 问题

`src/generator.js` 中存在大量硬编码数字，如 `randomInt(2, 6)`、`randomInt(3, 12)`、`randomInt(-5, 8)` 等，语义不清晰。

### 目标

将所有魔法数字提取为文件顶部的命名常量。

### 涉及文件

- **修改** `src/generator.js`（或新建 `src/generator-utils.js`）

### 具体做法

在 `generator.js`（或提取到 `generator-utils.js`）顶部定义常量区块：

```javascript
// ===== 难度参数常量 =====
/** 基础题型中答案的取值范围 */
const ANSWER_RANGE = Object.freeze({ min: 2, max: 18 });

/** 一步方程中系数的取值范围（乘法/除法） */
const ONE_STEP_COEFFICIENT_RANGE = Object.freeze({ min: 2, max: 9 });

/** 一步方程中常数的取值范围（加法/减法） */
const ONE_STEP_CONSTANT_RANGE = Object.freeze({ min: 3, max: 20 });

/** 未知数作减数时，被减数的取值范围 */
const SUBTRAHEND_LEFT_RANGE = Object.freeze({ min: 4, max: 20 });

/** 未知数作除数时，商的取值范围 */
const DIVISOR_QUOTIENT_RANGE = Object.freeze({ min: 2, max: 9 });

/** 两步方程中常数的取值范围 */
const TWO_STEP_CONSTANT_RANGE = Object.freeze({ min: 2, max: 15 });

/** 能算的先算中乘数的取值范围 */
const NUMBER_PART_FACTOR_RANGE = Object.freeze({ min: 2, max: 9 });
const NUMBER_PART_MULTIPLIER_RANGE = Object.freeze({ min: 2, max: 8 });

/** 括号方程中外层系数的取值范围 */
const BRACKET_OUTER_RANGE = Object.freeze({ min: 2, max: 8 });

/** 括号内常数的取值范围 */
const BRACKET_INNER_RANGE = Object.freeze({ min: -6, max: 8 });

/** 括号前减号题中外部数的取值范围 */
const MINUS_BRACKET_OUTER_RANGE = Object.freeze({ min: 18, max: 40 });

/** 合并同类项中系数的候选值 */
const COMBINE_COEFFICIENT_OPTIONS = Object.freeze([1.5, 2, 2.5, 3]);

/** 两边都有 x 题型中系数的取值范围 */
const X_BOTH_SIDES_RIGHT_RANGE = Object.freeze({ min: 1, max: 4 });
```

然后将代码中所有 `randomInt(2, 6)` 替换为 `randomInt(SOME_CONSTANT.min, SOME_CONSTANT.max)`。

#### 关键注意

- 使用 `Object.freeze` 防止意外修改。
- 每个常量加 JSDoc 注释说明其语义。
- 常量命名使用 `UPPER_SNAKE_CASE` 以区别于普通变量。

---

## P1-4: 补充边界条件测试

### 问题

当前测试覆盖了所有正常路径，但缺少以下边界条件：

1. 无效的 `typeId` 或 `categoryId`
2. `count` 为 0、负数、超大值、非数字
3. JSON 格式错误的请求体
4. 并发请求行为

### 目标

在现有测试文件中补充边界条件测试用例。

### 涉及文件

- **修改** `tests/generator.test.js`
- **修改** `tests/server.test.js`

### 具体做法

#### 在 `tests/generator.test.js` 中添加：

```javascript
test("handles invalid typeId gracefully by falling back to default", () => {
  const result = generateLocalProblems({
    count: 3,
    typeId: "nonexistent_type_xyz",
    includeSolution: true,
  });

  assert.equal(result.problems.length, 3);
  result.problems.forEach((problem) => {
    // 回退到 multiply_add_two_step 类型
    assert.equal(problem.typeId, "multiply_add_two_step");
    assertProblemIsValid(problem);
  });
});

test("handles invalid categoryId by generating from all types", () => {
  const result = generateLocalProblems({
    count: 3,
    categoryId: "nonexistent_category",
    includeSolution: true,
  });

  assert.equal(result.problems.length, 3);
  result.problems.forEach(assertProblemIsValid);
});

test("clamps count to valid range", () => {
  const tooLow = generateLocalProblems({ count: -5, difficulty: "easy" });
  assert.equal(tooLow.problems.length, 1); // clamped to min 1

  const tooHigh = generateLocalProblems({ count: 100, difficulty: "easy" });
  assert.equal(tooHigh.problems.length, 30); // clamped to max 30

  const nonNumber = generateLocalProblems({ count: "abc", difficulty: "easy" });
  assert.equal(nonNumber.problems.length, 10); // fallback to default 10
});

test("handles zero count by falling back to default", () => {
  const result = generateLocalProblems({ count: 0, difficulty: "easy" });
  assert.equal(result.problems.length, 10); // 0 clamped to min 1 then to 10? 需要确认 toPositiveInt 行为
});
```

#### 在 `tests/server.test.js` 中添加：

```javascript
test("returns 400 for malformed JSON body", async () => {
  const server = createServer({ env: {} });
  const baseUrl = await listen(server);

  try {
    const response = await fetch(`${baseUrl}/api/generate`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: "not valid json {{{",
    });
    assert.equal(response.status, 400);
    const body = await response.json();
    assert.equal(typeof body.error, "string");
  } finally {
    server.close();
  }
});

test("returns 400 for missing required fields", async () => {
  const server = createServer({ env: {} });
  const baseUrl = await listen(server);

  try {
    const response = await fetch(`${baseUrl}/api/generate`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({}), // 空对象
    });
    // 当前代码不会报错（使用默认值），但应确认行为符合预期
    const body = await response.json();
    assert.equal(response.status, 200); // 或 400，取决于期望行为
    assert.ok(body.problems.length > 0);
  } finally {
    server.close();
  }
});

test("returns 405 for unsupported HTTP methods", async () => {
  const server = createServer({ env: {} });
  const baseUrl = await listen(server);

  try {
    const response = await fetch(`${baseUrl}/api/generate`, { method: "GET" });
    assert.equal(response.status, 405);
  } finally {
    server.close();
  }
});

test("handles concurrent generate requests", async () => {
  const server = createServer({ env: {} });
  const baseUrl = await listen(server);

  try {
    const requests = Array.from({ length: 5 }, () =>
      fetch(`${baseUrl}/api/generate`, {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ count: 3, difficulty: "easy", includeSolution: true }),
      }).then((r) => r.json())
    );

    const results = await Promise.all(requests);
    results.forEach((result) => {
      assert.equal(result.source, "local");
      assert.equal(result.problems.length, 3);
    });
  } finally {
    server.close();
  }
});
```

---

## P2-1: 添加 JSDoc 类型注释

### 问题

项目没有任何类型定义，核心数据结构（`Problem`、`Equation`、`Category`）的形状只能通过阅读代码推断。

### 目标

为所有公开 API 和核心数据结构添加 JSDoc 注释。

### 涉及文件

- **修改** `src/generator.js`
- **修改** `server.js`

### 具体做法

#### 在 `src/generator.js` 顶部添加类型定义块：

```javascript
/**
 * @typedef {Object} Equation
 * @property {{ x: number, constant: number }} left  - 等式左边的 x 系数和常数项
 * @property {{ x: number, constant: number }} right - 等式右边的 x 系数和常数项
 */

/**
 * @typedef {Object} Problem
 * @property {string}  typeId      - 题型唯一标识
 * @property {string}  typeName    - 题型显示名称
 * @property {string}  focus       - 训练重点说明
 * @property {string}  question    - 题目文本
 * @property {Equation} equation   - 方程的内部表示（用于答案验证）
 * @property {number}  answer      - 方程的解
 * @property {string}  solution    - 解题过程（includeSolution=false 时为空字符串）
 * @property {string}  [mode]      - 题目模式: "concept" | "diagnosis" | "word_problem"
 * @property {string}  [conceptAnswer]  - 概念题答案（仅 mode=concept）
 * @property {string}  [wrongWork]      - 错误过程（仅 mode=diagnosis）
 * @property {string}  [errorPoint]     - 错误点说明（仅 mode=diagnosis）
 * @property {string}  [modelEquation]  - 列式模板（仅 mode=word_problem）
 */

/**
 * @typedef {Object} TypeDefinition
 * @property {string}   id          - 题型唯一标识
 * @property {string}   name        - 题型名称
 * @property {string}   pattern     - 题型通式
 * @property {string}   focus       - 训练重点
 * @property {string[]} examples    - 例题列表
 */

/**
 * @typedef {Object} CategoryDefinition
 * @property {string}           id          - 分类唯一标识
 * @property {string}           name        - 分类名称
 * @property {string}           description - 分类描述
 * @property {TypeDefinition[]} types       - 下属题型列表
 */
```

#### 为每个公开函数添加 JSDoc：

```javascript
/**
 * 根据题型定义生成一道练习题。
 *
 * @param {TypeDefinition} type            - 题型定义
 * @param {boolean}        [includeSolution=true] - 是否包含解析
 * @returns {Problem} 生成的题目对象
 */
function createEquationByType(type, includeSolution) { ... }

/**
 * 生成一批本地练习题（不使用 AI）。
 *
 * @param {Object} options
 * @param {number} [options.count=10]          - 题目数量（1-30）
 * @param {string} [options.typeId]            - 指定题型 ID
 * @param {string} [options.categoryId]        - 指定分类 ID
 * @param {string} [options.difficulty]        - 难度级别（easy/medium/hard）
 * @param {boolean} [options.includeSolution=true] - 是否包含解析
 * @returns {{ source: string, fallback: boolean, message: string, catalogVersion: string, problems: Problem[] }}
 */
function generateLocalProblems(options = {}) { ... }

/**
 * 根据用户关键词搜索匹配的题型。
 *
 * @param {string} query - 搜索关键词或方程例题
 * @returns {Array<TypeDefinition & { categoryId: string, categoryName: string, keywords: string[] }>}
 */
function searchTypes(query) { ... }
```

---

## P2-2: 修复 innerHTML 安全隐患

### 问题

`public/app.js:205-209` 使用 `innerHTML` 直接插入包含服务端数据的 HTML 字符串。

### 目标

改用 DOM API 构建 HTML，消除潜在的 XSS 风险（防御深度）。

### 涉及文件

- **修改** `public/app.js`

### 具体做法

将 `renderCoverageMap` 函数中的 `innerHTML` 替换为 DOM API：

```javascript
function renderCoverageMap() {
  const types = getAllTypes();
  coverageMap.replaceChildren();
  coverageSummary.textContent = `${catalog.length} 大专项，${types.length} 个细分题型；计算、应用题、错因诊断都在一个入口里。`;

  catalog.forEach((category) => {
    const card = document.createElement("article");
    card.className = "coverage-card";

    // 用 DOM API 替代 innerHTML
    const title = document.createElement("strong");
    title.textContent = category.name;

    const count = document.createElement("span");
    count.textContent = `${category.types.length} 个题型`;

    const typeList = document.createElement("div");
    typeList.className = "coverage-type-list";

    category.types.forEach((type) => {
      const button = document.createElement("button");
      button.type = "button";
      button.className = "coverage-type-button";
      button.textContent = type.name;  // textContent 自动转义，安全
      button.addEventListener("click", () => {
        selectType(category.id, type.id);
        searchInput.value = type.name;
        renderSearchResults();
      });
      typeList.appendChild(button);
    });

    card.appendChild(title);
    card.appendChild(count);
    card.appendChild(typeList);
    coverageMap.appendChild(card);
  });
}
```

---

## P2-3: 添加安全 HTTP 头和速率限制

### 问题

服务器未设置任何安全相关的 HTTP 响应头。

### 目标

为所有 HTTP 响应添加基础的 security headers。

### 涉及文件

- **修改** `server.js`

### 具体做法

#### 步骤 1：创建安全头中间件

在 `server.js` 顶部定义安全头对象：

```javascript
const SECURITY_HEADERS = {
  "X-Content-Type-Options": "nosniff",
  "X-Frame-Options": "DENY",
  "X-XSS-Protection": "0",  // 现代浏览器已弃用，但设为 0 表示不使用旧式过滤
  "Referrer-Policy": "strict-origin-when-cross-origin",
  "Permissions-Policy": "camera=(), microphone=(), geolocation=()",
};
```

#### 步骤 2：在 `sendJson` 中添加安全头

```javascript
function sendJson(response, statusCode, payload) {
  response.writeHead(statusCode, {
    "Content-Type": "application/json; charset=utf-8",
    ...SECURITY_HEADERS,
  });
  response.end(JSON.stringify(payload));
}
```

#### 步骤 3：在 `serveStatic` 中添加安全头

```javascript
response.writeHead(200, {
  "Content-Type": MIME_TYPES[extension] || "application/octet-stream",
  ...SECURITY_HEADERS,
});
```

注意：暂不添加 CSP（Content-Security-Policy），因为前端使用了 inline `<script>` 和 `innerHTML`。待重构后再加。

---

## P2-4: 添加请求速率限制

### 目标

为 `/api/generate` 端点添加简单的速率限制，防止滥用。

### 涉及文件

- **修改** `server.js`

### 具体做法

在 `server.js` 中添加基于 IP 的内存速率限制器：

```javascript
// 每秒每个 IP 最多请求数
const RATE_LIMIT = { windowMs: 1000, maxRequests: 5 };

/** @type {Map<string, { count: number, resetAt: number }>} */
const rateLimitMap = new Map();

// 定期清理过期记录（每 60 秒）
setInterval(() => {
  const now = Date.now();
  for (const [ip, record] of rateLimitMap) {
    if (now > record.resetAt) {
      rateLimitMap.delete(ip);
    }
  }
}, 60_000).unref(); // unref() 防止定时器阻止进程退出

function checkRateLimit(ip) {
  const now = Date.now();
  const record = rateLimitMap.get(ip);

  if (!record || now > record.resetAt) {
    rateLimitMap.set(ip, { count: 1, resetAt: now + RATE_LIMIT.windowMs });
    return true;
  }

  record.count += 1;
  return record.count <= RATE_LIMIT.maxRequests;
}

function getClientIp(request) {
  const forwarded = request.headers["x-forwarded-for"];
  if (forwarded) return forwarded.split(",")[0].trim();
  return request.socket.remoteAddress || "unknown";
}
```

然后在 `/api/generate` 处理中应用：

```javascript
if (request.method === "POST" && requestUrl.pathname === "/api/generate") {
  const ip = getClientIp(request);
  if (!checkRateLimit(ip)) {
    response.writeHead(429, { "Content-Type": "application/json; charset=utf-8" });
    response.end(JSON.stringify({ error: "请求过于频繁，请稍后再试。" }));
    return;
  }
  // ... 原有逻辑
}
```

---

## P3-1: 改进无障碍性

### 问题

快速标签按钮和题型全景图按钮缺少必要的 ARIA 属性。

### 目标

为交互元素添加适当的 ARIA 属性，提升屏幕阅读器体验。

### 涉及文件

- **修改** `public/index.html`
- **修改** `public/app.js`

### 具体做法

#### 在 `public/index.html` 中为快速标签添加 `aria-label`：

```html
<button type="button" data-query="未知数在分母" aria-label="搜索：未知数在分母">未知数在分母</button>
<button type="button" data-query="分母有x" aria-label="搜索：分母有 x">分母有 x</button>
<!-- 其余标签同样处理 -->
```

#### 在 `public/app.js` 的 `renderCoverageMap` 中为按钮添加 `aria-label`：

```javascript
button.setAttribute("aria-label", `${category.name} / ${type.name}`);
```

#### 在 `public/app.js` 的 `renderSearchResults` 中为结果按钮添加 `aria-label`：

```javascript
button.setAttribute("aria-label", `选择题型：${type.name}`);
```

#### 给消息区域添加 role：

```html
<p id="message" class="message" role="status" aria-live="polite">选择专项题型后，点击生成即可。</p>
```

注意：`role="status"` 隐式包含 `aria-live="polite"`，但显式加上 `aria-live="polite"` 兼容性更好。

---

## P3-2: 改进加载状态和错误提示

### 问题

题型目录加载失败时只有 `typeInfo` 中一行小字提示，用户容易忽略。没有任何骨架屏或 Loading 动画。

### 目标

改进加载和错误状态的视觉表现。

### 涉及文件

- **修改** `public/index.html`
- **修改** `public/app.js`
- **修改** `public/styles.css`

### 具体做法

#### 步骤 1：在 HTML 中添加全局错误横幅区域

```html
<div id="errorBanner" class="error-banner" role="alert" hidden>
  <span id="errorBannerText"></span>
  <button type="button" id="errorBannerDismiss" aria-label="关闭提示">&times;</button>
</div>
```

在 `<main>` 标签之后、`<script>` 之前加入。

#### 步骤 2：在 CSS 中添加错误横幅样式

```css
.error-banner {
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: #fef2f2;
  border: 1px solid #fecaca;
  color: #991b1b;
  padding: 12px 16px;
  margin-bottom: 16px;
  border-radius: 6px;
  font-weight: 600;
}

.error-banner[hidden] {
  display: none;
}

.error-banner button {
  background: none;
  border: 0;
  color: #991b1b;
  font-size: 20px;
  cursor: pointer;
  min-height: auto;
  padding: 0 4px;
}
```

#### 步骤 3：在 JS 中实现 `showError` 函数

```javascript
function showError(message) {
  const banner = document.querySelector("#errorBanner");
  const text = document.querySelector("#errorBannerText");
  text.textContent = message;
  banner.hidden = false;

  // 5 秒后自动隐藏
  clearTimeout(banner._timeout);
  banner._timeout = setTimeout(() => {
    banner.hidden = true;
  }, 5000);
}

document.querySelector("#errorBannerDismiss").addEventListener("click", () => {
  document.querySelector("#errorBanner").hidden = true;
});
```

#### 步骤 4：修改 `loadCatalog` 的错误处理

```javascript
async function loadCatalog() {
  try {
    const response = await fetch("/api/catalog");
    if (!response.ok) throw new Error(`加载失败 (${response.status})`);
    const data = await response.json();
    catalog = data.categories || [];
    renderAdvantageStats();
    renderCategories();
    renderTypes();
    renderCoverageMap();
    renderSearchResults();
  } catch (error) {
    showError("题型库加载失败，请刷新页面重试。");
    coverageSummary.textContent = "加载失败，请刷新页面。";
    typeInfo.textContent = "题型库加载失败，请刷新页面。";
  }
}
```

#### 步骤 5：在 `renderSearchResults` 的默认提示中使用样式而非纯文本

默认提示文字可以更醒目：

```css
.search-results p {
  color: var(--muted);
  font-size: 13px;
  line-height: 1.6;
  margin: 0;
  padding: 8px 0;
}
```

---

## 附录：改进任务的推荐执行顺序

| 顺序 | 任务 ID | 任务名称 | 预计工作量 | 风险 |
|------|---------|----------|-----------|------|
| 1 | P1-3 | 提取魔法数字 | 30 分钟 | 低 |
| 2 | P2-1 | 添加 JSDoc | 45 分钟 | 低 |
| 3 | P1-4 | 补充边界测试 | 1 小时 | 低（先写测试建立安全网） |
| 4 | P1-1 | 题型数据外置 JSON | 30 分钟 | 低 |
| 5 | P0-1 | 重构 switch-case | 2-3 小时 | **高**（核心重构，需要现有测试保护） |
| 6 | P0-2 | 消除搜索逻辑重复 | 1 小时 | 中 |
| 7 | P2-2 | 修复 innerHTML | 20 分钟 | 低 |
| 8 | P2-3 | 安全 HTTP 头 | 15 分钟 | 低 |
| 9 | P2-4 | 速率限制 | 30 分钟 | 低 |
| 10 | P3-1 | 改进无障碍性 | 30 分钟 | 低 |
| 11 | P3-2 | 改进加载/错误状态 | 30 分钟 | 低 |

**原则**：先做低风险、高收益的改进（提取常量、加类型注释、补充测试），在测试安全网的覆盖下再做核心重构。

---

> **文档版本**：v1.0 | **最后更新**：2026-06-24 | **基于项目版本**：0.1.0
