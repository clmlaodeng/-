{
  "name": "equation-practice-generator",
  "version": "0.1.0",
  "private": true,
  "description": "本地可运行的解方程练习生成器 MVP",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js",
    "verify": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
